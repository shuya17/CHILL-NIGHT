@AGENTS.md

# 夜景めぐり（仮）概要

## 1. アプリ概要

日本の夜景スポットに特化した「息抜き用チルWebアプリ」。写真を見て場所を当てるジオゲッサー的なミニゲームと、好きなスポットの景色をぼーっと眺められるアンビエントモードの2本柱で構成する。投稿・SNS的な要素は持たず、「見る・当てる・眺める」だけで完結する軽量アプリを目指す。

対象とする夜景は、日本三大夜景・新日本三大夜景・夜景100選など、知名度が高く「きれいで有名な景色」に絞る。マイナースポットは扱わない（当てにくくなりすぎる、写真素材が集めにくいため、またある程度有名どころのが身近に感じられるかと）。

## 2. ターゲット

- 作業や勉強の合間に軽く息抜きしたい学生・リモートワーカー
- 「何か作業中に流しっぱなしにする」「休憩中にちょっと遊ぶ」といった個人利用シーンを想定
- 複数人での共同編集・投稿・SNS的なやり取りは想定しない

## 3. MVP機能

### ① 夜景ジオゲッサー
- ランダムに日本の夜景スポット写真を表示
- 地図をクリックして場所を推測
- 正解地点との距離に応じてスコアを算出
- 答え合わせ画面で場所名・簡単な豆知識を表示

### ② アンビエントモード
- スポットを選択（またはおまかせ）
- そのスポットの時間帯別写真（朝／夕方／夜など）をクロスフェードで流し続ける
- 環境音（虫の声・波音など）を同時再生
- 操作不要、眺めるだけの「ながら見」機能

### ③ スポットデータ（裏側）
- 日本の有名夜景スポット20〜30件をキュレーション
- 写真・緯度経度・簡単な説明をセットで管理
- MVPでは静的JSONで保持し、①②で共通利用する

### MVPで含めないもの（Phase2以降）
- ユーザー投稿・アカウント機能
- スコアランキング／共有
- 時間指定＋タイマーでの詳細なタイムラプス制御
- ライブカメラ埋め込み
- プレイリスト選択機能（Spotify等との連携）

## 4. 技術スタック

| 領域 | 採用技術 | 備考 |
|---|---|---|
| フロントエンド | Next.js（React） | App Router想定 |
| スタイリング | Tailwind CSS または MUI | チームで統一して選定 |
| 地図表示 | Leaflet + OpenStreetMap | APIキー不要、クリックで座標取得しやすい |
| 音声再生 | HTML5 `<audio>` | ループ再生・クロスフェードはJSで制御 |
| データ管理 | 静的JSON（MVP） → 将来Supabase | 投稿機能が必要になった段階で移行 |
| デプロイ | Vercel

### 環境音・BGM素材の調達方針
- 効果音ラボ／DOVA-SYNDROME／甘茶の音楽工房（日本語の定番フリー素材サイト）
- Pixabay（Sound Effects / Music）
- Freesound.org（CC0ライセンスのみ使用）
- ※YouTube Audio Libraryは標準ライセンスがYouTube動画内使用に限定されるため使用しない
- ここAIが適当に選んだためまだ検討中
## 5. 開発体制・役割分担

基本全員がフロントエンドを担当し、自分はフロントエンドに加えて必要に応じてバックエンド対応（データ設計、将来のSupabase導入、デプロイ）を行う。

| 担当 | 画面／領域 | 必要な技術要素 |
|---|---|---|
| メンバーA | ジオゲッサー画面 | Leaflet（地図表示・クリック座標取得）、Haversine公式による距離計算、写真表示コンポーネント、スコアロジック、答え合わせUI |
| メンバーB | アンビエントモード画面 | CSSトランジション／JSタイマーによるクロスフェード、`<audio>`要素での環境音再生・切り替え、再生／停止コントロールUI |
| メンバーC | 共通UI・ナビゲーション | Tailwind CSS または MUIでのレイアウト構築、レスポンシブ対応、Next.js App Routerでのページ遷移 |
| 自分 | 全体設計・バックエンド | プロジェクト全体設計、GitHubリポジトリ／ブランチ運用ルール整備、スポットデータ（JSON）設計、必要時のSupabase導入、デプロイ |

データ収集（スポット写真・緯度経度・説明文、環境音素材）は特定の担当を決めず、Git研修も兼ねて全員で持ち寄る。

## 6. 進め方

### フェーズ0：環境構築＆Git研修（メンバー3人向け）
1. VSCodeインストール
2. Node.js／npmインストール
3. Gitインストール＋GitHubアカウント作成
4. スターターリポジトリ（Next.js＋Tailwind/MUI雛形）をclone → `npm install` → `npm run dev` でローカル起動確認
5. 練習用の小さなPRを1本出し、branch作成→commit→push→Pull Request→マージの流れを体験

### フェーズ1：MVP実装
- Week1：フェーズ0（環境構築＋Git研修）＋スポットデータ・音素材の収集開始
- Week2〜3：各画面の実装（ジオゲッサーとアンビエントモードを並行）
- Week4：結合・スタイリング調整・デプロイ


## 規約
- コンポーネントはapp/配下に機能ごとに分ける
- コミットメッセージは日本語でOK